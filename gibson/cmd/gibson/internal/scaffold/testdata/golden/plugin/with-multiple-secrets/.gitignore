# Gibson plugin — generated by `gibson component init`

# Host key — persisted by `gibson component register`; never committed.
host_key
host_key.json
~/.gibson/plugin/byte-identity-multi/

# Build artifacts
/byte-identity-multi
/dist/

# Go toolchain
*.test
*.out
coverage.html
