# Gibson plugin — generated by `gibson component init`

# Host key — persisted by `gibson component register`; never committed.
host_key
host_key.json
~/.gibson/plugin/byte-identity-secret/

# Build artifacts
/byte-identity-secret
/dist/

# Go toolchain
*.test
*.out
coverage.html
